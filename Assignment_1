1. Write a program (WAP) to display "Hello World" on console display.

#include <iostream>
using namespace std;

int main() 
{
    cout << "Hello World";
    return 0;
}

2. WAP to input an integer, decimal and character and display it.

#include <iostream>
using namespace std;

int main() 
{
    int a;
    float d;
    char c;

    cout << "Enter integer: ";
    cin >> a;
    cout << "Enter decimal: ";
    cin >> d;
    cout << "Enter character: ";
    cin >> c;

    cout << "Integer = " << a << endl;
    cout << "Decimal = " << d << endl;
    cout << "Character = " << c << endl;

    return 0;
}

3. WAP to add, subtract, multiply and divide numbers.

#include <iostream>
using namespace std;

int main() 
{
    int a,b;

    cout << "Enter two numbers: ";
    cin >> a >> b;

    cout << "Addition = " << a + b << endl;
    cout << "Subtraction = " << a - b << endl;
    cout << "Multiplication = " << a * b << endl;
    cout << "Division = " << a / b << endl;

    return 0;
}

4. WAP to convert Celsius to Fahrenheit.

#include <iostream>
using namespace std;

int main() 
{
    float c,f;

    cout << "Enter temperature in Celsius: ";
    cin >> c;

    f = (9 * c / 5) + 32;

    cout << "Temperature in Fahrenheit = " << f << endl;

    return 0;
}

5. WAP to format console output using \n, \t, \b, endl, setw.

#include <iostream>
#include <iomanip>
using namespace std;

int main() 
{
    cout << "Hello\nWorld" << endl;
    cout << "Hello\tWorld" << endl;
    cout << "Hello\bWorld" << endl;
    cout << setw(10) << "Hello World" << endl;

    return 0;
}

6. WAP to utilize assignment += and -= operators.

#include <iostream>
using namespace std;

int main() 
{
    int a=10;

    a+=5;
    cout << "After += : " << a << endl;

    a-=3;
    cout << "After -= : " << a << endl;

    return 0;
}

7. WAP to swap two numbers using bitwise operator.

#include <iostream>
using namespace std;

int main() 
{
    int a,b;

    cout << "Enter two numbers: ";
    cin >> a >> b;

    a=a^b;
    b=a^b;
    a=a^b;

    cout << "After swapping:" << endl;
    cout << "1st number = " << a << endl;
    cout << "2nd number = " << b << endl;

    return 0;
}

8. Library fine calculation program.

#include <iostream>
using namespace std;

int main() 
{
    int days;
    float fine=0;

    cout << "Enter number of days late: ";
    cin >> days;

    if (days<=5)
    {
        fine=days*0.50;
    }
    else if (days<=10 && days>5)
    {
        fine=(days-5)*1;
        fine+=2.5;
    }
    else if (days<=30 && days>10)
    {
        fine=(days-10)*5;
        fine+=7.5;
    }
    else 
    {
        cout << "Membership cancelled" << endl;
        return 0;
    }

    cout << "Fine = " << fine << " rupees" << endl;

    return 0;
}

9. WAP to display arithmetic operations using switch-case.

#include <iostream>
using namespace std;

int main() 
{
    int a,b,choice;

    cout << "Enter two numbers: ";
    cin >> a >> b;

    cout << "1. Add\n2. Subtract\n3. Multiply\n4. Divide\n";
    cout << "Enter choice: ";
    cin >> choice;

    switch (choice) {
        case 1: cout << "Result = " << a + b; break;
        case 2: cout << "Result = " << a - b; break;
        case 3: cout << "Result = " << a * b; break;
        case 4: cout << "Result = " << a / b; break;
        default: cout << "Invalid choice";
    }

    return 0;
}

10. WAP to check whether a number is even or odd.

#include <iostream>
using namespace std;

int main() 
{
    int n;

    cout << "Enter a number: ";
    cin >> n;

    if (n%2==0)
        cout << "Even number";
    else
        cout << "Odd number";

    return 0;
}
